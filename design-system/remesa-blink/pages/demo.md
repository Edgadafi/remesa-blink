# /demo override — Remesa Blink

Master: `design-system/remesa-blink/MASTER.md`

Shares `EmpezarArt` with `/empezar`. Slogan in HTML: **Send dollars, recibe pesos sin salir de WhatsApp.**

- Background: SVG corridor + USD → WhatsApp → pesos. No corridor PNG.
- QR card (WhatsApp) then **Blink preview** (Dialect-style receipt) on Papel `#fffdf8`.
- Action GET is holatia.app, never trycloudflare.
