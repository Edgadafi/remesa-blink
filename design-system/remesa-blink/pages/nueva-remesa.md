# /nueva-remesa override — Remesa Blink

Master: `design-system/remesa-blink/MASTER.md`

## Intent

Form to program a family remesa. Same landing chrome as `/` (papel, nopal sticky header, rounded cards). Copy unchanged.

## Chrome

- `HubPageIntro` uses `landing-pride` / `landing-h1` / `landing-section-lede`.
- Form cards: `#fffdf8`, 16px radius, nopal 12% border.
- No corridor backdrop.
