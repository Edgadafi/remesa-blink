import { LogoIntro } from "@/components/LogoIntro";

export const metadata = {
  title: "Intro — holatia",
  description: "Cinematic logo intro preview",
};

export default function IntroPage() {
  return <LogoIntro />;
}
